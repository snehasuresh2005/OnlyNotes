# Production backend (Render). Loaded for `npm run build` via .env.production.
# In development, leave unset — Vite proxies /api to http://localhost:3001.
VITE_API_URL=https://onlynotes-xeat.onrender.com
