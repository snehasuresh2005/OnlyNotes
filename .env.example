# Server Configuration (server/.env)
PORT=3001
JWT_SECRET=your_jwt_secret_here_change_in_production

# No API keys needed — AI features use built-in text analysis

# Frontend (client/.env.production or Vercel env vars)
VITE_API_URL=https://onlynotes-xeat.onrender.com
